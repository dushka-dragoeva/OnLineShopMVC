@model string

@{
    string classToUse = "form-control";
    //string validationClass="te"
    // custom class is passed in view as anonimus object  @Html.TextBoxFor(m=>m.Name, new {customclass="alabala"})
    //if (ViewData["customclass"] != null)
    //{
    //    classToUse = (string)ViewData["customclass"];

    //}

    if (ViewBag.CustomClass != null)
    {
        classToUse = (string)ViewBag.CustomClass;

    }
}

<div class="row form-control-static">
    @Html.LabelFor(m => m)
    <br />
    @Html.ValidationMessageFor(model => model, "     ", new { @class = "text -danger" })
    @Html.TextBoxFor(m => m, new { placeholder = ViewData.ModelMetadata.Watermark, @class = "form - control" })
</div >
<br />
